<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.co.vermore.mapper.UserForeignMapper">
    <resultMap id="UserForeignResult" type="jp.co.vermore.entity.UserForeign">
        <result column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="foreign_type" property="foreignType"/>
        <result column="foreign_id" property="foreignId"/>
        <result column="is_login" property="isLogin"/>
        <result column="access_token" property="accessToken"/>
        <result column="refresh_token" property="refreshToken"/>
        <result column="token_secret" property="tokenSecret"/>
        <result column="password" property="password"/>
        <result column="expire_datetime" property="expireDatetime"/>
        <result column="data" property="data"/>
        <result column="create_datetime" property="createDatetime"/>
        <result column="update_datetime" property="updateDatetime"/>
        <result column="del_flg" property="delFlg"/>
        <result column="note" property="note"/>
    </resultMap>

    <sql id="Base_Column_List">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Wed Mar 21 17:21:40 JST 2018.
        -->
        id, user_id, foreign_type, foreign_id, is_login, access_token, refresh_token, token_secret,
        password, expire_datetime, data, create_datetime, update_datetime, del_flg, note
    </sql>

    <insert id="insertUserForeign">
        insert into user_foreign(
        user_id,
        foreign_type,
        foreign_id,
        is_login,
        access_token,
        refresh_token,
        token_secret,
        password,
        expire_datetime,
        create_datetime,
        update_datetime,
        del_flg,
        note
        ) values (
        #{userId},
        #{foreignType},
        #{foreignId},
        #{isLogin},
        #{accessToken},
        #{refreshToken},
        #{tokenSecret},
        #{password},
        #{expireDatetime},
        #{createDatetime},
        #{updateDatetime},
        #{delFlg},
        #{note}
        )
    </insert>

    <!--<select id="getByMail" resultMap="UserForeignResult" parameterType="string">-->
    <select id="getByMail" resultMap="UserForeignResult" parameterType="string">
        select
        <include refid="Base_Column_List"/>
        from user_foreign
        where foreign_id = #{mail} order by user_id desc limit 1
    </select>

    <select id="getByForeignId" resultMap="UserForeignResult" parameterType="map">
        select
        <include refid="Base_Column_List"/>
        from user_foreign
        where foreign_id=#{foreignId} order by user_id desc limit 1
    </select>

    <select id="getByForeignTypeAndForeignId" resultMap="UserForeignResult" parameterType="map">
        select
        <include refid="Base_Column_List"/>
        from user_foreign
        where foreign_type=#{foreignType} and foreign_id=#{foreignId} order by user_id desc limit 1
    </select>

    <select id="getByUserId" resultMap="UserForeignResult" parameterType="long">
        select
        <include refid="Base_Column_List"/>
        from user_foreign
        where user_id=#{userId} order by user_id desc limit 1
    </select>

    <select id="getUserForeignByUserIdList" resultMap="UserForeignResult" parameterType="java.util.List">
        select
        <include refid="Base_Column_List"/>
        from user_foreign
        where user_id in
        <foreach collection="list" item="ent" separator="," open="(" close=")">
            #{ent}
        </foreach>
        order by user_id
    </select>

    <select id="getUserForeignByUserIdAndForeignType" resultMap="UserForeignResult" >
        select
        <include refid="Base_Column_List"/>
        from user_foreign
        where
        user_id = #{param1}
        and foreign_type = #{param2}
        and del_flg = 0
        order by user_id desc
    </select>

    <select id="getByForeignTypeAndForeignIdAndPassword" resultMap="UserForeignResult" parameterType="map">
        select
        <include refid="Base_Column_List"/>
        from user_foreign
        where foreign_type=#{foreignType} and foreign_id=#{foreignId} and password=#{password} order by user_id desc limit 1
    </select>

    <update id="updateByPrimaryKey" parameterType="jp.co.vermore.entity.UserForeign" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Wed Mar 28 23:54:06 JST 2018.
        -->
        update user_foreign
        set user_id = #{userId,jdbcType=INTEGER},
        foreign_type = #{foreignType,jdbcType=TINYINT},
        foreign_id = #{foreignId,jdbcType=VARCHAR},
        is_login = #{isLogin,jdbcType=TINYINT},
        access_token = #{accessToken,jdbcType=VARCHAR},
        refresh_token = #{refreshToken,jdbcType=VARCHAR},
        token_secret = #{tokenSecret,jdbcType=VARCHAR},
        password = #{password,jdbcType=VARCHAR},
        expire_datetime = #{expireDatetime,jdbcType=TIMESTAMP},
        create_datetime = #{createDatetime,jdbcType=TIMESTAMP},
        update_datetime = #{updateDatetime,jdbcType=TIMESTAMP},
        del_flg = #{delFlg,jdbcType=BIT},
        note = #{note,jdbcType=VARCHAR}
        where id = #{id,jdbcType=INTEGER}
    </update>

    <delete id="deleteByUserId"  >
        update user_foreign set del_flg = 1 where user_id = #{userId}
    </delete>

</mapper>