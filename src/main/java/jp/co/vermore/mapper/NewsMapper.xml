<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="jp.co.vermore.mapper.NewsMapper">
    <resultMap id="BaseResultMap" type="jp.co.vermore.entity.News">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Wed Feb 28 15:54:28 CST 2018.
        -->
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="uuid" property="uuid" jdbcType="VARCHAR"/>
        <result column="date" property="date" jdbcType="TIMESTAMP"/>
        <result column="type" property="type" jdbcType="TINYINT"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="sort_score" property="sortScore" jdbcType="INTEGER"/>
        <result column="excerpt" property="excerpt" jdbcType="VARCHAR"/>
        <result column="publish_start" property="publishStart" jdbcType="TIMESTAMP"/>
        <result column="publish_end" property="publishEnd" jdbcType="TIMESTAMP"/>
        <result column="create_datetime" property="createDatetime" jdbcType="TIMESTAMP"/>
        <result column="update_datetime" property="updateDatetime" jdbcType="TIMESTAMP"/>
        <result column="del_flg" property="delFlg" jdbcType="BIT"/>
        <result column="note" property="note" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Wed Feb 28 15:54:28 CST 2018.
        -->
        id, uuid, date, type, title, excerpt, publish_start, publish_end, create_datetime, update_datetime,
        del_flg, note, sort_score
    </sql>

  <select id="getNews" resultMap="BaseResultMap" parameterType="int">
    select id
    from news
    where id = #{id}
  </select>

    <select id="getNewsByUuid" resultMap="BaseResultMap" parameterType="String">
    select *
    from news
    where uuid = #{uuid}
  </select>

    <select id="getNewsPre" resultMap="BaseResultMap" >
    select *
    from news
    where date &lt; #{param1}
    and `type` between 1 and 3
    and del_flg = 0
    and publish_start &lt; #{param3}
    and publish_end &gt; #{param2}
    order by date desc
    LIMIT 1;
  </select>

    <select id="getNewsNext" resultMap="BaseResultMap" >
    select *
    from news
    where date &gt; #{param1}
    and `type` between 1 and 3
    and del_flg = 0
    and publish_start &lt; #{param3}
    and publish_end &gt; #{param2}
    order by date asc
    LIMIT 1;
  </select>

    <select id="getNewsCategory" resultMap="BaseResultMap" parameterType="int">
    select uuid,date,type,title,excerpt
    from news
    where type = #{param1}
     LIMIT #{param2} OFFSET #{param3};
  </select>

  <select id="getNewsJsonAll" resultMap="BaseResultMap">
    select uuid,date,type,title,sort_score,excerpt
    from news
    where del_flg = 0 and `type` between 1 and 3
      and publish_start &lt; #{param3}
      and publish_end &gt; #{param2}
    <if test="param1 != 0 and param1 != 4 and param1 != 5" >
      and type = #{param1}
    </if>
     order by `date` desc
     LIMIT #{param4} OFFSET #{param5};
  </select>

  <select id="getNewsJsonAllByType" resultMap="BaseResultMap"  >
    select uuid,date,type,title,excerpt
    from news
    where del_flg = 0 and `type` between 1 and 3
      and publish_start &lt; #{param3}
      and publish_end &gt; #{param2}
    <if test="param1 != 0" >
    and type = #{param1}
    </if>
    order by `date` desc
  </select>

  <select id="getNewsByIdAndType" resultMap="BaseResultMap" >
    select uuid,date,type,title,excerpt
    from news
    where del_flg = 0
    <if test="param1 != 0" >
      and id = #{param1}
    </if>
    <if test="param2 != 0" >
      and type = #{param2}
    </if>
  </select>

    <select id="getNewsEventAll" resultMap="BaseResultMap">
        select uuid,date,type,title,sort_score,excerpt
        from news
        where del_flg = 0
        and publish_start &lt; #{param3}
        and publish_end &gt;= #{param4}
        <if test="param1 != 0">
            and type = #{param1} OR type = #{param2}
        </if>
        LIMIT #{param5} OFFSET #{param6};
    </select>

    <select id="getNewsAll" resultMap="BaseResultMap">
    select uuid,date,type,title,sort_score,excerpt
    from news
    where  `type` between 1 and 3 and del_flg = 0
    order by date desc
  </select>

    <select id="getNewsAllForTop" resultMap="BaseResultMap">
    select uuid,date,type,title,excerpt
    from news
    where  `type` between 1 and 3 and del_flg = 0
    and publish_start &lt; #{param2}
    and publish_end &gt; #{param1}
    order by sort_score desc,`date`desc, create_datetime desc
    LIMIT 8
  </select>

    <insert id="insertNews">
        insert into news(
        uuid,
        date,
        type,
        title,
        sort_score,
        excerpt,
        publish_start,
        publish_end,
        create_datetime,
        update_datetime,
        del_flg,
        note
        ) values (
        #{uuid},
        #{date},
        #{type},
        #{title},
        #{sortScore},
        #{excerpt},
        #{publishStart},
        #{publishEnd},
        #{createDatetime},
        #{updateDatetime},
        #{delFlg},
        #{note}
        )
        <selectKey resultType="Long" keyProperty="id" order="AFTER">
            select @@IDENTITY
        </selectKey>
    </insert>

    <delete id="deleteNews">
    update news set del_flg = #{delFlg} where id = #{id}
  </delete>

    <update id="updateNews">
       update news
       set
        date = #{date},
        type = #{type},
        title = #{title},
        sort_score = #{sortScore},
        excerpt = #{excerpt},
        publish_start = #{publishStart},
        publish_end = #{publishEnd},
        create_datetime = #{createDatetime},
        update_datetime = #{updateDatetime},
        del_flg = #{delFlg},
        note = #{note}
       where id = #{id}
   </update>

    <select id="getNewsList" resultMap="BaseResultMap">
    select id,uuid,date,type,title,sort_score,excerpt,publish_start,publish_end
    from news
    where del_flg = 0
    and id = #{id}
  </select>

    <select id="getNewsAllByCondition" parameterType="jp.co.vermore.form.admin.NewsListForm" resultMap="BaseResultMap">
        select *
        from news
        where del_flg = 0 and `type` between 1 and 3
        <if test="type != null and type != 9 and type != 4 and type != 5">
            and `type` = #{type}
        </if>
        <if test="date != null">
            and date_format(`date`,'%Y-%m-%d') =date_format(#{date},'%Y-%m-%d')
        </if>
        <if test="sortScore != null and sortScore != '' ">
            and sort_score = #{sortScore}
        </if>
        <if test="publishStart != null">
            and date_format(publish_start,'%Y-%m-%d') &gt;= date_format(#{publishStart},'%Y-%m-%d')
        </if>
        <if test="publishEnd != null">
            and date_format(publish_end,'%Y-%m-%d') &lt;= date_format(#{publishEnd},'%Y-%m-%d')
        </if>
        order by ${orderStatement} ,sort_score desc,`date`desc, create_datetime desc
        LIMIT #{length,jdbcType=INTEGER} OFFSET #{start,jdbcType=INTEGER};
    </select>

    <select id="getNewsCountByCondition" parameterType="jp.co.vermore.form.admin.NewsListForm" resultType="int">
        select count(id) as totalCount
        from news
        where del_flg = 0 and `type` between 1 and 3
        <if test="type != null and type != 9 and type != 4 and type != 5">
            and `type` = #{type}
        </if>
        <if test="date != null">
            and date_format(`date`,'%Y-%m-%d') =date_format(#{date},'%Y-%m-%d')
        </if>
        <if test="sortScore != null and sortScore != ''">
            and sort_score = #{sortScore}
        </if>
        <if test="publishStart != null">
            and date_format(publish_start,'%Y-%m-%d') &gt;= date_format(#{publishStart},'%Y-%m-%d')
        </if>
        <if test="publishEnd != null">
            and date_format(publish_end,'%Y-%m-%d') &lt;= date_format(#{publishEnd},'%Y-%m-%d')
        </if>
    </select>

    <select id="getNewsCount" resultType="int">
    select count(id) as totalCount
    from news
    where `type` between 1 and 3 and del_flg = 0;
  </select>

    <select id="getStudioNewsList" resultMap="BaseResultMap" parameterType="jp.co.vermore.entity.News">
        select
        <include refid="Base_Column_List"/>
        from news
        where del_flg = 0
        <if test="param1 != null">
            and type = #{param1}
        </if>
        and publish_start &lt; #{param3}
        and publish_end &gt;= #{param4}
        order by sort_score desc , `date`desc
        limit 5;
    </select>

    <select id="getStudioNewsListAll" resultMap="BaseResultMap" parameterType="jp.co.vermore.entity.News">
        select
        <include refid="Base_Column_List"/>
        from news
        where del_flg = 0
        <if test="param1 != null">
            and type = #{param1}
        </if>
        order by sort_score desc , `date`desc
        LIMIT #{param2} OFFSET #{param3};
    </select>

    <select id="getStudioNewsALL" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from news
        where del_flg = 0
        and type = #{type}
        order by date desc
    </select>

    <select id="getStudioAllByCondition" parameterType="jp.co.vermore.form.admin.NewsListForm" resultMap="BaseResultMap">
        select *
        from news
        where del_flg = 0
        and type = 5
        <if test="date != null">
            and date_format(`date`,'%Y-%m-%d') =date_format(#{date},'%Y-%m-%d')
        </if>
        <if test="sortScore != null and sortScore != '' ">
            and sort_score = #{sortScore}
        </if>
        <if test="publishStart != null">
            and date_format(publish_start,'%Y-%m-%d') &gt;= date_format(#{publishStart},'%Y-%m-%d')
        </if>
        <if test="publishEnd != null">
            and date_format(publish_end,'%Y-%m-%d') &lt;= date_format(#{publishEnd},'%Y-%m-%d')
        </if>
        order by ${orderStatement}
        LIMIT #{length,jdbcType=INTEGER} OFFSET #{start,jdbcType=INTEGER};
    </select>

    <select id="getStudioCountByCondition" parameterType="jp.co.vermore.form.admin.NewsListForm" resultType="int">
        select count(id) as totalCount
        from news
        where del_flg = 0
        and type = 5
        <if test="date != null">
            and date_format(`date`,'%Y-%m-%d') =date_format(#{date},'%Y-%m-%d')
        </if>
        <if test="sortScore != null and sortScore != ''">
            and sort_score = #{sortScore}
        </if>
        <if test="publishStart != null">
            and date_format(publish_start,'%Y-%m-%d') &gt;= date_format(#{publishStart},'%Y-%m-%d')
        </if>
        <if test="publishEnd != null">
            and date_format(publish_end,'%Y-%m-%d') &lt;= date_format(#{publishEnd},'%Y-%m-%d')
        </if>
    </select>

    <select id="getStudioCount" resultType="int">
        select count(id) as totalCount
        from news
        where del_flg = 0
        and type = 5;
    </select>

  <select id="getNewsById" resultMap="BaseResultMap">
    SELECT *
    FROM news
    WHERE id = #{id}
    and del_flg = 0;
  </select>
</mapper>